/**
 * Thoregon Repository
 * - Only Thoregon system components
 * - public repository -> Ammandul
 *
 * Permissions: Archetim
 *
 *
 * @author: Bernhard Lukassen
 */

export default class Dorifer {

    constructor({
                    id
                } = {}) {
        Object.assign(this, { id });
    }

}
